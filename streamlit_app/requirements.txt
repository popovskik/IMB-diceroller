# Streamlit Community Cloud installs from THIS file because it sits next to the
# entrypoint (streamlit_app.py) and that location takes precedence over the repo root.
streamlit==1.41.1
requests==2.32.3
