# Copy this file to secrets.toml (gitignored) for local runs.
# On Streamlit Community Cloud, paste the same content into
# Manage app -> Settings -> Secrets (do NOT commit the real key).
OPENWEATHER_API_KEY = "your_openweathermap_api_key_here"
