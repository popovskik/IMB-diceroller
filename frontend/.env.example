# Optional. Leave unset/empty to call the same-origin /api (Vercel + VPS + dev-proxy).
# Set only if your backend lives on a DIFFERENT origin than the frontend, e.g.:
# VITE_API_BASE=https://api.example.com
VITE_API_BASE=
